"use client";
import { FormEvent,useEffect,useMemo,useState } from "react";
import { Edit3,Plus,RefreshCw,Search,Tags,Trash2,X } from "lucide-react";
import toast from "react-hot-toast";
import { adminService,type Brand } from "@/lib/api/endpoints/admin";
const slugify=(v:string)=>v.toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"");
export default function AdminBrands(){
 const [rows,setRows]=useState<Brand[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(""),[name,setName]=useState(""),[slug,setSlug]=useState(""),[busy,setBusy]=useState(false),[query,setQuery]=useState(""),[editing,setEditing]=useState<Brand|null>(null);
 const load=async()=>{setLoading(true);setError("");try{setRows(await adminService.listBrands())}catch(e){setError(e instanceof Error?e.message:"Unable to load brands.")}finally{setLoading(false)}};useEffect(()=>{void load()},[]);
 const visible=useMemo(()=>{const q=query.trim().toLowerCase();return rows.filter(r=>`${r.name} ${r.slug}`.toLowerCase().includes(q))},[rows,query]);
 const create=async(e:FormEvent)=>{e.preventDefault();if(!name.trim())return;setBusy(true);try{await adminService.createBrand({name:name.trim(),slug:slug.trim()||slugify(name)});setName("");setSlug("");toast.success("Brand created.");await load()}catch(e){toast.error(e instanceof Error?e.message:"Unable to create brand.")}finally{setBusy(false)}};
 const save=async()=>{if(!editing)return;setBusy(true);try{await adminService.updateBrand(editing.id,{name:editing.name.trim(),slug:editing.slug.trim()});toast.success("Brand updated.");setEditing(null);await load()}catch(e){toast.error(e instanceof Error?e.message:"Unable to update brand.")}finally{setBusy(false)}};
 const remove=async(r:Brand)=>{if(!confirm(`Delete brand "${r.name}"?`))return;setBusy(true);try{await adminService.deleteBrand(r.id);toast.success("Brand deleted.");await load()}catch(e){toast.error(e instanceof Error?e.message:"Unable to delete brand.")}finally{setBusy(false)}};
 return <div className="space-y-5"><section className="rounded-2xl border bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-[.14em] text-[#f47524]">Catalog brands</p><h2 className="mt-1 text-2xl font-bold">Brand Management</h2><p className="mt-1 text-sm text-gray-500">Add, edit and delete brands available to sellers.</p></section><div className="grid gap-5 xl:grid-cols-[390px_minmax(0,1fr)]"><form onSubmit={create} className="h-fit rounded-2xl border-2 bg-[#f8fafc] p-5 shadow-sm"><div className="flex items-center gap-3"><span className="rounded-xl bg-orange-50 p-2 text-[#f47524]"><Tags size={18}/></span><div><h3 className="font-bold">Add New Brand</h3><p className="text-xs text-gray-500">Create a selectable product brand.</p></div></div><Field label="Brand name"><input value={name} onChange={e=>{setName(e.target.value);if(!slug)setSlug(slugify(e.target.value))}} placeholder="e.g. Samsung" className="field"/></Field><Field label="Slug"><input value={slug} onChange={e=>setSlug(slugify(e.target.value))} placeholder="samsung" className="field"/></Field><button disabled={busy||!name.trim()} className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-[#111827] py-3 text-sm font-semibold text-white disabled:opacity-50"><Plus size={15}/>Add Brand</button></form><section className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="flex items-center justify-between border-b p-5"><div><h3 className="font-bold">Brands</h3><p className="text-xs text-gray-500">{rows.length} registered</p></div><div className="flex gap-2"><div className="relative"><Search size={15} className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search brands..." className="h-10 rounded-xl border pl-9 pr-3 text-sm"/></div><button onClick={()=>void load()} className="rounded-xl border p-2"><RefreshCw size={15}/></button></div></div>{loading?<p className="p-10 text-center">Loading...</p>:error?<p className="p-10 text-center text-red-600">{error}</p>:<table className="w-full text-left text-sm"><thead className="bg-gray-50"><tr>{["Name","Slug","Created","Actions"].map(x=><th key={x} className="px-5 py-3">{x}</th>)}</tr></thead><tbody className="divide-y">{visible.map(r=><tr key={r.id}><td className="px-5 py-4 font-semibold">{r.name}</td><td className="px-5 py-4 text-gray-500">{r.slug}</td><td className="px-5 py-4 text-gray-500">{new Date(r.created_at).toLocaleDateString()}</td><td className="px-5 py-4"><div className="flex gap-3"><button onClick={()=>setEditing({...r})} className="inline-flex items-center gap-1 text-xs font-semibold text-blue-600"><Edit3 size={13}/>Edit</button><button onClick={()=>void remove(r)} className="inline-flex items-center gap-1 text-xs font-semibold text-red-600"><Trash2 size={13}/>Delete</button></div></td></tr>)}</tbody></table>}</section></div>{editing&&<div className="fixed inset-0 z-[100] flex items-center justify-center bg-black/50 p-4"><div className="w-full max-w-md rounded-2xl bg-white p-6"><div className="flex justify-between"><h3 className="font-bold">Edit Brand</h3><button onClick={()=>setEditing(null)}><X size={18}/></button></div><Field label="Brand name"><input value={editing.name} onChange={e=>setEditing({...editing,name:e.target.value})} className="field"/></Field><Field label="Slug"><input value={editing.slug} onChange={e=>setEditing({...editing,slug:e.target.value})} className="field"/></Field><button disabled={busy||!editing.name.trim()} onClick={()=>void save()} className="mt-5 w-full rounded-xl bg-[#111827] py-3 font-semibold text-white">Save Changes</button></div></div>}<style jsx global>{`.field{margin-top:.5rem;min-height:46px;width:100%;border-radius:.75rem;border:2px solid #d8e0e9;background:#fff;padding:.7rem .9rem;font-size:.875rem;outline:none}.field:focus{border-color:#f47524}`}</style></div>
}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="mt-4 block"><span className="text-sm font-semibold text-[#334155]">{label}</span>{children}</label>}
