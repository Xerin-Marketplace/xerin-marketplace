"use client";
import { useEffect,useState } from "react";
import Link from "next/link";
import toast from "react-hot-toast";
import { ordersApi } from "@/lib/api/endpoints/commerce";
import type { Order } from "@/types/api/commerce";
import { formatCurrency } from "@/lib/formatCurrency";
import UnavailableFeature from "@/components/Admin/Common/UnavailableFeature";

export default function OrderDetails({orderId}:{orderId:string}){const[order,setOrder]=useState<Order|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const load=async()=>{setLoading(true);setError("");try{setOrder(await ordersApi.get(orderId))}catch(cause){setOrder(null);setError(cause instanceof Error?cause.message:"Unable to load order.")}finally{setLoading(false)}};useEffect(()=>{void load()},[orderId]);const update=async()=>{if(!order)return;const status=window.prompt("New status: pending, paid, processing, shipped, delivered, cancelled or refunded",order.status)?.trim();if(!status||status===order.status)return;try{await ordersApi.updateStatus(order.id,{status});toast.success("Order status updated.");await load()}catch(cause){toast.error(cause instanceof Error?cause.message:"Status update failed.")}};if(loading)return <p className="p-10 text-center text-gray-500">Loading order...</p>;if(error||!order)return <div className="p-10 text-center"><p className="text-red-600">{error||"Order not found."}</p><button onClick={()=>void load()} className="mt-3 font-semibold text-orange-600">Retry</button></div>;return <div className="space-y-5"><section className="rounded-2xl border bg-white p-6 shadow-sm"><Link href="/admin/orders" className="text-sm font-semibold text-orange-600">← Back to orders</Link><div className="mt-4 flex justify-between gap-4"><div><p className="text-xs uppercase text-gray-500">Order ID</p><h2 className="text-2xl font-semibold">{order.id}</h2><p className="mt-1 text-sm text-gray-500">{order.created_at?new Date(order.created_at).toLocaleString():"Date unavailable"}</p></div><button onClick={()=>void update()} className="h-fit rounded-xl bg-gray-800 px-4 py-2.5 text-sm font-semibold text-white">Update status</button></div><dl className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Status",order.status],["Subtotal",formatCurrency(order.subtotal,order.currency)],["Discount",formatCurrency(order.discount_amount,order.currency)],["Total",formatCurrency(order.total,order.currency)]].map(([label,value])=><div key={label} className="rounded-xl bg-gray-50 p-4"><dt className="text-xs text-gray-500">{label}</dt><dd className="mt-1 font-semibold capitalize">{value}</dd></div>)}</dl></section><section className="overflow-hidden rounded-2xl border bg-white shadow-sm"><h3 className="border-b p-5 font-semibold">Order items</h3><table className="w-full text-left text-sm"><thead className="bg-gray-50"><tr><th className="px-5 py-3">Product</th><th className="px-5 py-3">Quantity</th><th className="px-5 py-3">Unit price</th><th className="px-5 py-3">Total</th></tr></thead><tbody className="divide-y">{order.items.map(item=><tr key={item.id}><td className="px-5 py-4 font-semibold">{item.product_name}</td><td className="px-5 py-4">{item.quantity}</td><td className="px-5 py-4">{formatCurrency(item.unit_price,order.currency)}</td><td className="px-5 py-4">{formatCurrency(item.total_price,order.currency)}</td></tr>)}</tbody></table></section><UnavailableFeature title="Tracking, cancellation and order-level refunds are not available yet" description="The backend currently exposes order details and a shared status mutation only. Tracking details, cancellation workflow and order-level refund endpoints are not simulated." /></div>}
